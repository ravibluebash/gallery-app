<div class='container'>
<h4>Edit tags</h1>
<%# <%= form_with(model: @tag, url: [:admin, @album], local: true) do |form| %> 
<%= form_with(model: [:admin, @album, @tag ], local: true, method: :put) do |form| %>
  <%= render partial: 'form', locals: { f: form } %>
<% end %>
<%= link_to 'Back' , admin_albums_path ,class:"btn btn-outline-success btn-pill px-3"%>
</div>