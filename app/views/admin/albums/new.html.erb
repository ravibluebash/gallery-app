<div class='container'>
<%= link_to 'Back', admin_albums_path ,style:"float: right;" ,class:"btn btn-outline-primary btn-pill px-3  "%>
</div>
<div class='container'>
<h3>New Album</h3>
<%# <%= form_with scope: :album, url: admin_albums_path, method: :post ,local: true do |form| %>
<%= form_with(model: [:admin, @album], local: true, method: :post) do |form| %>
<%= render partial: 'form', locals: { f: form } %>
</div>
<%end%>

